{
  "name": "gamedao-haiku",
  "description": "a monorepo for gamedao dapps",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "setup": "yarn && wsrun -s -c install",
    "dev": "wsrun -p @gamedao/app -c dev",
    "build": "wsrun -e -c -s -x -m build",
    "test": "wsrun -e -c -s --exclude-missing test",
    "release": "yarn build && yarn changeset publish",
    "format": "prettier --parser babel-ts --write --check '{apps,packages}/**/*.{ts,tsx}'",
    "format:verify": "prettier --parser babel-ts --check '{apps,packages}/**/*.{ts,tsx}'",
    "format:staged": "pretty-quick --staged --pattern '{apps,packages}/**/*.{ts,tsx}'",
    "lint": "yarn lint:app",
    "lint:app": "yarn eslint 'apps/**/src'",
    "build:graphql": "yarn workspace @gamedao/app generate",
    "build:frontend": "yarn workspace @gamedao/app build",
    "build:admin": "yarn workspace @gamedao/admin build",
    "prod:frontend": "yarn npm-run-all -s build:graphql build:frontend",
    "commit": "git-cz",
    "analyze": "ANALYZE=true wsrun -p @gamedao/app -c build",
    "sb": "wsrun -p @gamedao/app -c storybook",
    "reset": "rm -rf node_modules apps/**/node_modules packages/**/node_modules"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@changesets/cli": "^2.23.2",
    "@commitlint/cli": "^17.4.4",
    "@commitlint/config-conventional": "^17.4.4",
    "@commitlint/cz-commitlint": "^17.4.4",
    "@picgo/bump-version": "^1.0.3",
    "@trivago/prettier-plugin-sort-imports": "^4.1.1",
    "@typescript-eslint/utils": "^5.55.0",
    "commitizen": "^4.3.0",
    "conventional-changelog": "^3.1.24",
    "cz-conventional-changelog": "^3.3.0",
    "cz-customizable": "^7.0.0",
    "eslint": "^8.36.0",
    "eslint-plugin-graphql": "^4.0.0",
    "eslint-plugin-storybook": "^0.6.11",
    "husky": "^8.0.1",
    "lint-staged": "^13.2.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.8.4",
    "pretty-quick": "^3.1.3",
    "rimraf": "^4.4.0",
    "wsrun": "^5.2.1"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  },
  "husky": {
    "hooks": {
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS",
      "pre-commit": "yarn lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": "yarn format"
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  },
  "eslintConfig": {
    "rules": {
      "@next/next/no-img-element": "off"
    }
  }
}
