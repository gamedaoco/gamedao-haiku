import { TabPanel } from '@mui/lab'
import { Typography } from '@mui/material'

export const DashboardTab = () => {
	// select organization
	// select battlepass

	// set available free passes
	// set available premium passes

	// see statistics:
	// - total users
	// - total free passes
	// - total premium passes

	return (
		<TabPanel sx={{ py: 2 }} value="dashboard">
			<Typography></Typography>
		</TabPanel>
	)
}
