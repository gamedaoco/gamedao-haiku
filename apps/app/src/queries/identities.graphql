subscription IdentityByAddress($address: String!) {
    # identityByPk(id: $address) {
	identity_by_pk(id: $address) {
        id
        email
        display_name
        image
        legal_name
        riot
        twitter
        web
        web3name
        discord
    }
}
