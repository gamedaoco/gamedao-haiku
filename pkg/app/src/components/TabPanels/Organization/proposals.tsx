import { Typography } from '@mui/material'

interface ComponentProps {
	organizationId: string
}

export function Proposals({ organizationId }: ComponentProps) {
	return <Typography>Proposals</Typography>
}
