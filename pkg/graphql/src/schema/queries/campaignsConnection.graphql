query CampaignsConnection ($address: String!) {
  campaignsConnection(orderBy: createdAtBlock_DESC, where: {adminIdentity: {address_eq:  $address}}) {
       edges {
          node {
            createdAtBlock
            deposit
            expiry
            target
            state
            metadata {
              logo
              name
              title
            }
            contributors {
              id
            }
          }
    }
  }
}

