query Organizations($limit: Int, $orderBy: [OrganizationOrderByInput]) {
  organizations(limit: $limit, orderBy: $orderBy) {
        id
        metadata {
            name
            description
            logo
        }
        members {
            address
            identity {
                displayName
            }
        }
        cid
        access
    }
}
